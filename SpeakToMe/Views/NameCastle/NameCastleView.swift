import SwiftUI

struct NameCastleView: View {
    var body: some View {
        ActivityGameView(
            worldTitle: "Name Castle",
            categoryHint: "Identity",
            mascot: "🏰",
            draggableEmoji: "🔠"
        )
    }
}
